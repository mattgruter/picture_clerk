PictureClerk creates checksum for important picture files. The script hashing_performance.py calculates checksums for supplied files and prints the execution time for MD5 and SHA1 hashing alternatives.

SHA1 is about as fast as MD5 while being cryptographically more secure.
